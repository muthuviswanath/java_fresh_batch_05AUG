package randomnumbers;

public class PubG {
public static void main(String[] args) {
	/*Generate the random number between 1 and 3
	If the generated random number is 1, then create the object of Knife and
	call the useWeapon method that was overidden to showcase the functionality
	
	If the generated random number is 2, then create the object of Grenade and
	call the useWeapon method that was overidden to showcase the functionality
	
	If the generated random number is 3, then create the object of Sniper and
	call the useWeapon method that was overidden to showcase the functionality
	*/
	
}
}
